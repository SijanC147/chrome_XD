# Project Context

**IMPORTANT**: At the start of every session, register the active project with serena from the name of the directory, then run `/sc:load .` to load the project memory context from Serena.

This loads all necessary project context and background information.
